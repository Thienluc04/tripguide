export enum UserGender {
  Male,
  Female,
}

export enum Language {
  English,
  Vietnamese,
}

export enum UserStatus {
  Unverified,
  Verified,
  Banned,
}

export enum MediaType {
  Image,
  Video,
}
